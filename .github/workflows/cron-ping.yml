name: Cron Ping

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Hit URL
        env:
          CRON_PING_URL: ${{ secrets.CRON_PING_URL }}
        run: curl -s --fail "$CRON_PING_URL"
