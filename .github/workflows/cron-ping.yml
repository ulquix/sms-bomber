name: Cron Ping

on:
  schedule:
    - cron: '0,10,20,30,40,50 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Hit URL
        env:
          CRON_PING_URL: ${{ secrets.CRON_PING_URL }}
        run: curl -s --fail "$CRON_PING_URL"
